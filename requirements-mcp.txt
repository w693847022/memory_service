# MCP Server 核心依赖
# 用于 MCP Server 容器构建

mcp[cli]==1.26.0
pydantic==2.12.5
aiofiles==25.1.0
pyyaml==6.0.3
rich==14.3.3
tomli>=2.0.0  # fallback for Python < 3.11
cachetools>=5.3.0  # TTL cache for memory optimization
requests==2.32.2  # HTTP 客户端（features/search.py 需要）
httpx==0.27.2  # Business API HTTP 客户端
